version: '3.9'

services:
  app:
    ports:
      - "11000:11000"
    build:
      context: .
      dockerfile: .docker/Dockerfile.dev
      args:
        # replace for your personal access token
        - devops_token=<%= config.token %>
    volumes:
    - ./:/app
    privileged: true
    stdin_open: true
    tty: true
    working_dir: /app
    entrypoint: make all